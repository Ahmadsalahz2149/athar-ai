// cPanel Passenger looks for app.js by default. The Next.js standalone output
// provides server.js, so keep this adapter intentionally minimal.
// eslint-disable-next-line @typescript-eslint/no-require-imports
require("./server.js");
