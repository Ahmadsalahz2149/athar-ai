# CLOUD_HANDOFF — أثر AI (Athar)

> ملف تسليم لجلسة **Continue in Cloud**. الهدف: نقل عبء التطوير (بناء/اختبار/كود) إلى بيئة Linux سحابية لتخفيف الضغط على جهاز المؤسس. آخر تحديث: 2026-09-09.

---

## 1. الحالة الآن (أين وصلنا بالضبط)

- **الإنتاج شغّال**: `https://athargrowth.com` — تسجيل الدخول يعمل بعد إصلاح عطل DNS في الخادم اليوم (كان الخادم يترجم `supabase.co` إلى `::1`؛ صُلح الجذر في `/etc/resolv.conf` + قُفل، وأُزيل التثبيت المؤقت). لا علاقة لهذا بالكود.
- **الفرع**: `main`، الشجرة **نظيفة** ومتزامنة مع `origin/main` (0 ahead / 0 behind). آخر commit: `c2ebf6d` (تشخيص `/api/health?probe=auth` + رفع كاش الـ PWA).
- **آخر عمل كودي**: إعادة تصميم الملاحة إلى «٥ عوالم» + مراحل، وإصلاح كراش المستخدم الجديد (23505)، وتلميحات المستخدم الجديد.
- **لا توجد مهمة كود عالقة نصف مكتملة** — نقطة تسليم نظيفة.

## 2. نوع المشروع والـ Stack (مفحوص، غير مفترَض)

- **Next.js 16.3.0** (App Router, Turbopack للبناء) + **React 19.2** + **TypeScript**.
- تعدد اللغات عبر **next-intl** (`/ar` افتراضي RTL، `/en` LTR).
- قاعدة البيانات: **Postgres (Supabase)** عبر **Drizzle ORM** (هجرات في `drizzle/`).
- بحث دلالي: **pgvector** + embeddings من **Voyage**.
- ذكاء اصطناعي: **Anthropic** (افتراضي) أو **MiniMax**؛ تفريغ صوت/فيديو عبر **ElevenLabs**.
- طابور مهام خلفية خاص (جدول `background_jobs`) يُستنزف عبر `POST /api/worker` (محمي بـ `WORKER_SECRET`).
- اختبارات: **Vitest**. لينـت: **ESLint** (فيه قاعدة تفرض واجهة الـ tenancy، ADR-005).
- **مدير الحزم: npm** (يوجد `package-lock.json` فقط). **Node 22** (CI يستخدم 22؛ لا يوجد `.nvmrc`).
- **لا يوجد Docker** ولا docker-compose.

## 3. ما يحتاجه المشروع (تشغيل/بناء/اختبار)

### أوامر فعلية (من `package.json`)
```bash
npm ci            # تثبيت نظيف
npm run lint      # ESLint (يشمل قاعدة الـ tenancy)
npm run build     # next build — يشمل فحص الأنواع + التصريف
npm run test      # vitest run --no-file-parallelism
npm run eval      # tsx eval/run.ts — بوابة جودة (تصرف توكنز؛ تحتاج ANTHROPIC_API_KEY)
npm run db:generate | db:migrate | db:push   # Drizzle — تلمس Postgres حقيقي
```

### المتغيّرات (أسماء فقط — القيم في `.env.local`/`.env.production` وهي **غير موجودة في الريبو**، مُتجاهَلة عبر `.gitignore`)
- **حمّالة (بدونها تنكسر ميزات بصمت):** `DATABASE_URL`, `ANTHROPIC_API_KEY`, `VOYAGE_API_KEY`, `ELEVENLABS_API_KEY`, `WORKER_SECRET`
- **Supabase/المصادقة:** `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `OAUTH_BASE_URL`
- **مزوّد/نماذج:** `AI_PROVIDER`, `ANTHROPIC_DNA_MODEL`, `ANTHROPIC_DRAFT_MODEL`, `MINIMAX_*` (اختياري), `VOYAGE_MODEL`, `ELEVENLABS_STT_MODEL`
- **إدارة/أعلام:** `ADMIN_EMAILS`, `NEXT_PUBLIC_ENABLE_GOOGLE_OAUTH`, `NEXT_PUBLIC_ENABLE_LINKEDIN_OAUTH`
- المرجع الكامل بالتعليقات: `.env.example`.

### خدمات خارجية
Supabase (Postgres/Auth/Storage) · Anthropic · Voyage · ElevenLabs · (MiniMax اختياري). لا شيء منها مُتاح تلقائيًا في الكلاود؛ يحتاج مفاتيح.

## 4. تقسيم العمل: كلاود vs محلي

### ✅ يصلح في الكلاود (Linux، بدون جهاز المؤسس)
- كتابة/تعديل الكود، `npm run lint`، `npm run build`، فحص الأنواع.
- **الاختبارات النقية** (rubric + حساب الكريدت) تعمل بدون أسرار. اختبارات tenancy/credit-ledger **تحتاج** `DATABASE_URL` (Postgres تجريبي مؤقت) وإلا تُتخطّى.
- عمل المستندات في `docs/`، والريفاكتور، وكتابة الاختبارات.
- فتح Pull Requests على `main`.

### 🖥️ يبقى محليًا/يدويًا عند المؤسس (ولماذا)
- **النشر للإنتاج**: عبر SSH إلى خادم Hostinger cPanel كمستخدم `athar` ثم `sh scripts/deploy-cpanel.sh`. **لا يُنفَّذ من الكلاود** — يحتاج وصول الخادم، وسياستنا: النشر بموافقة المؤسس فقط.
- **الخطوات اليدوية في اللوحات**: SMTP في Supabase، Redirect URLs، مراقبة UptimeRobot/Sentry، Backups — لوحات ويب لا كود (راجع `docs/LAUNCH_READINESS.md`).
- **DNS/الخادم**: أي إصلاح بنية تحتية (مثل عطل اليوم) على الخادم مباشرة.
- **التحقق البصري من الإنتاج** (تسجيل دخول حقيقي في المتصفح) — عند المؤسس.
- **هجرات قاعدة البيانات على الإنتاج** (`db:migrate`/`db:push` ضد Postgres الحقيقي) — لا تُشغَّل من الكلاود.

## 5. مهام مرشّحة للكلاود (مرتبة — من `docs/LAUNCH_READINESS.md`، الجزء الكودي فقط)

> هذه أعمال **كود** يمكن للكلاود إنجازها كـ PRs؛ الخطوات اليدوية (اللوحات) تبقى للمؤسس.

1. **ربط Sentry** لالتقاط الأخطاء (الـ stream routes تبلع الأخطاء بصمت حاليًا) — A6.
2. **تدقيق الصدق المتبقّي**: التأكد أن أزرار السوشيال/حالات «Published» لا توهم بالنشر الفعلي؛ إخفاء/تفعيل الأزرار الميتة — A3.
3. **مراجعة/تحسين الصفحات القانونية** (Terms/Privacy/Refund) نصًّا — A4 (المراجعة القانونية تبقى بشرية).
4. **دفع self-serve** (Stripe/Paddle/Moyasar) كمرحلة لاحقة — A5 (تصميم + كود؛ المفاتيح لاحقًا).
5. صيانة عامة: رفع تغطية الاختبارات، تنظيف نصوص غير مستخدمة، تحديث `README.md` (حاليًا افتراضي من create-next-app).

> ملاحظة: كثير من إصلاحات الصدق/القانوني الأساسية **أُنجزت ونُشرت** بالفعل (راجع LAUNCH_READINESS). ليؤكّد الكلاود الحالة الفعلية قبل إعادة العمل.

## 6. القيود والمحاذير الخاصة بهذا المشروع

- ⚠️ **Next.js مُعدّلة**: `AGENTS.md` يقول إن هذه النسخة فيها breaking changes — **يجب قراءة `node_modules/next/dist/docs/` قبل كتابة أي كود Next**. لا تعتمد على معرفة سابقة بـ Next.
- ⚠️ **`next dev` يعيد كتابة كتلة في `AGENTS.md`**: تشغيل خادم التطوير يُحدث تغييرًا غير مرفوع في `AGENTS.md`. في الكلاود فضّل `npm run build` للتحقق؛ ولو شغّلت `dev` فاعمل commit للتغيير مع عملك حتى تبقى الشجرة نظيفة.
- ⚠️ **الأسرار ليست في الريبو**: أي عمل يتجاوز البناء/اللينت يحتاج ضبط متغيّرات البيئة كأسرار في بيئة الكلاود. استخدم **قواعد/مفاتيح تجريبية (throwaway)**، لا أسرار الإنتاج.
- ⚠️ **عزل العملاء (tenancy)**: كل وصول للبيانات يمر عبر واجهة `forOrg(db, orgId)`؛ قاعدة ESLint (ADR-005) تفرضها — لا تلتف حولها.
- ⚠️ **لا نشر تلقائي للسوشيال** بعد (سياسة مقصودة لتفادي حظر المنصات) — لا تبنِ أتمتة نشر كاملة.
- ⚠️ **لا تنشر للإنتاج ولا تعدّل الـ remote/الأسرار من الكلاود**. سلّم عبر PR.

## 7. معايير القبول (كيف نعرف أن الشغل خلص صح)

عمل الكلاود يُعتبر مقبولًا عندما:
- [ ] `npm run lint` ينجح بلا أخطاء.
- [ ] `npm run build` ينجح (فحص الأنواع + التصريف) بلا أخطاء.
- [ ] `npm run test` أخضر (الاختبارات النقية على الأقل؛ واختبارات DB إن توفّر `DATABASE_URL` تجريبي).
- [ ] الشجرة نظيفة (بما فيها كتلة `AGENTS.md` إن شُغّل `dev`).
- [ ] التغيير مُقدَّم كـ **Pull Request** على `main` بوصف واضح — **دون** نشر للإنتاج ودون لمس الأسرار.
- [ ] أي ادعاء «شغّال» مدعوم بمخرجات فعلية (سجل بناء/اختبار)، تماشيًا مع أسلوب العمل validation-first.

## 8. قبل النقل — مسؤولية المؤسس

1. تأكيد نظافة الشجرة والتزامن (أمر تحقق في الرسالة).
2. **push** لهذا الملف بعد commit (حتى تراه نسخة الكلاود).
3. ضبط أسرار الكلاود (متغيّرات البيئة أعلاه) — من واجهة الكلاود، لا من الشيل.
4. الضغط على **Continue in Cloud**.
